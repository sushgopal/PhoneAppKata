<?xml version="1.0" encoding="utf-8"?>

<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/activity_main"
    android:layout_height="match_parent"
     android:layout_width="match_parent"
    android:orientation="vertical">

    <TextView
        android:text="Number Of Rows"
        android:id="@+id/number_of_rows_label"
        android:textSize="36sp"
        android:textAlignment="center"
        android:layout_height="wrap_content"
        android:layout_width="match_parent" />

    <NumberPicker
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/row_picker" />

    <TextView
        android:text="Number Of Columns"
        android:id="@+id/number_of_columns_label"
        android:textSize="36sp"
        android:textAlignment="center"
        android:layout_height="wrap_content"
        android:layout_width="match_parent" />

    <NumberPicker
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/column_picker"/>

    <Button
        android:text="Submit"
        android:id="@+id/submit_grid_size"
        android:layout_height="wrap_content"
        android:layout_width="match_parent"
        android:onClick="submitGridSize"/>

</LinearLayout>
